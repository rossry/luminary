# Luminary 2.1 runtime dependencies
numpy>=2.0
pydantic>=2.5

# Web server (spec §15) and WebSocket driver
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Serial driver (spec §12.2)
pyserial>=3.5

# Mapping persistence (plan/mapping/DESCRIPTION.md "Saved state")
pyyaml>=6.0

# Development / testing
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
websockets>=12.0     # live WS end-to-end tests
mypy>=1.10
black>=24.0
types-pyserial
types-PyYAML
